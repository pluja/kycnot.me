---
import { markdownToHtml } from '../lib/markdown'

type Props = {
  content: string
  allowImages?: boolean
}

const { content, allowImages } = Astro.props

const html = await markdownToHtml(content, { allowImages })
---

<Fragment set:html={html} />
