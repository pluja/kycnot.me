# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# local only data
local_data/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
# .env.staging and .env.production are committed (build-time public values
# only; real secrets are injected at runtime by docker compose).
.env
.env.local
.env.*.local

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

local_uploads/
!local_uploads/.gitkeep
uploads/